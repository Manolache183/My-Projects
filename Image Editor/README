Manolache Marius-Alexandru 313CA

	Programul se bazeaza pe analizarea cu fgets a comenzii din stdin si spargerea pe stringuri diferite in functie de delimitatorii "space" si "newline", fiind blocat intr-o bucla infinita pana cand este executata comanda "EXIT". LOAD si SAVE au fost cele mai greu de implementat. Pentru load m-am folosit de variabila b si de ordinea if-urilor pentru a sti exact unde ma aflu in fisier, fisier deschis ca si text pana la citirea matricei, unde in cazul in care aceasta este stocata binar il redeschid. fscanf pentru citirea plain, fread pentru cea binara, cate un byte char stocat apoi intr-un int. La scrierea din SAVE am optat pentru fprintf si fputc pentru antet atat la binar cat si la ascii iar apoi pentru matricea binara am scris cu fwrite cate un byte. 
	Am salvat si imaginea color tot intr-o singura matrice, asa mi s-a parut mai comod, punand un *3 acolo unde e cazul. Pentru CROP doar am salvat intr-o copie elementele din selectia actuala, apoi am realocat matricea imagine, urmand sa pun la loc elementele selectate si sa updatez dimnesiunile. La SELECT doar am analizat cei 4 parametrii, verificand primul caracterele care trebuia sa fie reprezinte o cifra, cu exceptia primului care putea fi '-'. La ROTATE m-am folosit tot de o copie unde am salvat elementele ce trebuiesc rearanjate. SEPIA si GRAYSCALE au fost cele mai ok. 
	Zi faina!
	
